<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{#if dashboard}}Your Dashboard{{else}}The Tech Deck{{/if}}</title>
    <link href="https://fonts.googleapis.com/css?family=Roboto:400,400i,700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="/css/styles.css">
    <!-- TailwindCSS -->
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
</head>

<body class="flex flex-col min-h-screen bg-gradient-to-r from-gray-900 via-gray-700 to-gray-900 text-white">
    <nav class="bg-gray-800 p-4">
        <ul class="flex justify-center space-x-4">
            <li><a href="/" class="text-white hover:text-gray-300">Home</a></li>
            {{#if logged_in}}
            <li><a href="/dashboard" class="text-white hover:text-gray-300">Dashboard</a></li>
            <li><a href="#" id="logout" class="text-white hover:text-gray-300">Logout</a></li>
            {{else}}
            <li><a href="/login" class="text-white hover:text-gray-300">Login</a></li>
            <li><a href="/signup" class="text-white hover:text-gray-300">Sign Up</a></li>
            {{/if}}
        </ul>
    </nav>

    <header class="bg-gray-700 text-center py-10">
        <h1 class="font-sans text-4xl">
            <a href="{{#if dashboard}}/dashboard{{else}}/{{/if}}" class="font-bold text-white hover:text-gray-300">
                {{#if dashboard}}Your Dashboard{{else}}The Tech Deck{{/if}}
            </a>
        </h1>
    </header>

    <main class="flex-grow container mx-auto p-4">
        {{{body}}}
    </main>

    <footer class="bg-gray-800 text-center p-4 mt-auto">
        {{#if dashboard}}
        <a href="/dashboard/new" class="text-white hover:text-gray-300">➕ New Post</a>
        {{else}}
        <p>&copy; 2024 The folks here at Tech Deck thank you for your contributions</p>
        {{/if}}
    </footer>

    {{#if logged_in}}
    <script src="/js/logout.js"></script>
    {{/if}}
    <script src="/js/autoLogout.js"></script>
</body>

</html>
